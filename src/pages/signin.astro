---
import PageLayout from '~/layouts/PageLayout.astro';
import SignIn from '../components/authentication/sigin';
---

<PageLayout metadata={{ title: 'Entrar al portal de socios' }}>
  <div class="h-[calc(100vh-var(--header-height)-var(--footer-height))]">
    <section class="relative w-full h-full flex items-center justify-center mt-20">
      <SignIn client:load />
    </section>
  </div>
</PageLayout>
